import PaperLanding from "@/app/components/landing/PaperLanding";

// Temporary preview of the new neo-brutalist landing while it's built section by
// section. The real swap into / happens at Step 6 (see CLAUDE.md §4).
export default function PreviewPage() {
  return <PaperLanding />;
}
